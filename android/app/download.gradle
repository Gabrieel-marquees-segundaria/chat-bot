import de.undercouch.gradle.tasks.download.Download

tasks.register("downloadAAR", Download) {
    src project.findProperty("AAR_URL") ?: ""
    dest project.findProperty("AAR_PATH") ?: ""
    overwrite(false)
}
